diff --git a/docs/doc/en/3.4.-Attributes.md b/docs/doc/en/3.4.-Attributes.md
index 9698d95..ab78298 100644
--- a/docs/doc/en/3.4.-Attributes.md
+++ b/docs/doc/en/3.4.-Attributes.md
@@ -8,9 +8,9 @@ Attributes are variables associated with files, cases or journals. They can be u
 
 You can right-click on a numeric attribute to convert to Character.
 
-The only options for attributes are numeric and character. There are no options for boolean, date or time or specific numeric types such as integer.  However, if you are running SQL queries, you may need to cast the numeric attribute to an Integer or Real: cast(my_var as integer).
+The only options for attributes are numeric and character. There are no options for boolean, date or time or specific numeric types such as integer.  However, if you are running SQL queries, via Reports > Database queries, you may need to cast the numeric attribute to an Integer or Real, e.g. cast(my_var as integer).
 
-Attributes can be added in the Manage Files, Manage Cases or Manage Journals sections by clicking on the (x) button, shown below. 
+Attributes can be added in the Manage Attributes, Manage Files, Manage Cases or Manage Journals sections by clicking on the (x) button, shown below. 
 
 Attributes can be imported from a survey, and also from an REFI-QDA project file. Note REFI-QDA project import is not guaranteed to successfully import.
 
@@ -20,7 +20,7 @@ Attributes can also be added or deleted using the Manage > Manage Attributes opt
 
 ![image](https://github.com/user-attachments/assets/513af5f1-a94e-4606-b4d0-54011a8607da)
 
-In this area you can delete attributes. But even though you can delete reference attributes, we recommend that you do not do this, unless you are certain all your references are not needed. These are prefixed with 'Ref_' and are associated with imported refernces that link a file to a specific refernce.
+In this area you can delete attributes. But even though you can delete reference attributes, we recommend that you do not do this, unless you are certain all your references are not needed. These are prefixed with 'Ref_' and are associated with imported references that link a file to a specific reference. See Manage References to import and link references to files.
 
 
 
diff --git a/docs/doc/en/3.5.-References.md b/docs/doc/en/3.5.-References.md
new file mode 100644
index 0000000..67d66c1
--- /dev/null
+++ b/docs/doc/en/3.5.-References.md
@@ -0,0 +1,21 @@
+# References
+
+Go to Manage > References to import a bibliography. This must be in RIS or NBIB formatted plain text. The file can have endings of .txt (for.ris) .ris or .nbib
+
+<img width="1361" height="704" alt="image" src="https://github.com/user-attachments/assets/caff300e-f3e6-47c1-a8a1-ff4ff74281b2" />
+
+
+Press the import button, and select a bibliography file (.txt, .nbib., .ris).
+
+The Action Log will show how many references were imported.
+
+Now you can link (and unlink) references to files. You can manually edit an imported reference. You can delete a reference.
+
+In the Manage Files screen there will be four new attribute columns: Ref_Authors, Ref_Title, Ref_Type, Ref_Year
+
+<img width="840" height="162" alt="image" src="https://github.com/user-attachments/assets/3d85d09f-926c-46df-87cb-867ad8dce4c1" />
+
+Files linked to references will have details in those columns.
+
+References can be displayed in analysis - such as the Analysis - Code Retrieval
+
diff --git a/docs/doc/en/4.1.-Coding-Text.md b/docs/doc/en/4.1.-Coding-Text.md
index aba2f17..c6db05a 100644
--- a/docs/doc/en/4.1.-Coding-Text.md
+++ b/docs/doc/en/4.1.-Coding-Text.md
@@ -42,7 +42,7 @@ Overlapping codes can be difficult to view clearly. Overlapping sections are und
 ## Annotations
 Add an annotation (a memo for a text segment) to a text selection. These are useful for explaining acronyms and jargon. The text will become bold to mark the position of the annotation. To re-open an annotation, select some of the bolded-text and right-click to get the Annotate option. Alternatively, click the leftmost notepad and pencil button, or press the 'A' key. To remove an annotation, open the annotation and delete all the text.
 
-![annotation](https://qualcoder.files.wordpress.com/2019/01/annotation.png?w=429&h=240)
+<img width="400" height="160" alt="image" src="https://github.com/user-attachments/assets/104526a8-1bba-4dae-8bcd-43d2ddc5f9c8" />
 
 ## Auto-code
 
@@ -72,33 +72,23 @@ There is an undo button to remove the auto-codings that were last applied.
 
 **Emoji's , emoticons in text and auto-coding**
 
- Raw emoji characters might count as 1 character in a string but take up 2 or more positions in the visual editor, leading to incorrect calculations. Applying auto-code means that there will be a shifting of character postions when applying auto-code to the text.
+ Raw emoji characters might count as 1 character in a string but take up 2 or more positions in the visual editor, leading to incorrect calculations. Applying auto-code means that there _might_ be a shifting of character postions when applying auto-code to the text.
 
  Example auto-coding the word 'music', each emoji is shown as 'one'character, but behind the scenes it is 2 characters. This may result in a shift, seen below:
  
 ![image](https://github.com/user-attachments/assets/8ac5bcf5-fae4-4da3-a1dd-8dccf12d585b)
 
-_____________
-
-!!! warning
-    **Auto-code Exact Text ERRORS IDENTIFIED in version 3.8, upgrade to version 4.0 or 3.8.2**
-    **When auto-coding exact text and selecting multiple files,the autocoding process stops, as soon as it finds a file with no matching text, and does not continue with the subsequent files.**
-    **When auto-coding exact text with REGEX the regex text is stored in the coding segment, and not the correct text identfied using REGEX. This solution works best if there  are no emoji's in the text. Solution where this has occured:  Back up your project first and then run an update in Reports > Database Queries using this command :** 
-    ```
-    update code_text set seltext = (select substr(source.fulltext, code_text.pos0 + 1, code_text.pos1-code_text.pos0) from source where code_text.fid=source.id)
-    ```
-    Another alternative is to upgrade to 4.0 or 3.8.2 from 3.8, delete all the incorrect Regex and run the regex again.
 
 ## Auto speaker codings
 
 The function "mark speakers" creates codes for every speaker in an interview or group discussion, and assigns utterances automatically. This allows for more focussed reports, only looking at certain speakers at a time. You can reach the function via the file context menu or the following toolbar button: 
 
-![grafik](https://github.com/user-attachments/assets/c742fad3-f34b-471d-8794-1919b7f5935d)
+<img width="650" height="500" alt="image" src="https://github.com/user-attachments/assets/645f0a43-4814-466e-a8a0-10758b910bb0" />
 
 - The speaker name must be at the start of a new line and less than 64 characters long. The following formats are detected: "Name:", "{Name} or "[Name]"
-- You can select which names you want to include and even edit the label: 
+- You can select which names you want to include and even edit the label:
 
-![speaker selection dialog](https://github.com/user-attachments/assets/055a2ffa-7ab5-40f6-a5e1-a9bb3a1c42de)
+<img width="650" height="500" alt="image" src="https://github.com/user-attachments/assets/67c14d0f-2c9a-4e99-8907-9c16a32b6285" />
 
 - Each speaker will get its own code in the special category "📌 Speakers" at the very bottom of the code tree. The default colors are from the gray spectrum, to make them less distracting.
 - However, you can still choose to hide these codings in the text window, so that they don't distract you in your own analytic coding. To do this, click on the "..." button right next to the coder name in the top left of the toolbar. In the following dialog, select the "📌 Speaker coding" entry and set the visibility to "hidden".
@@ -126,12 +116,14 @@ Some projects may have many text files and the view file dialog may present too
 
 ![image](https://github.com/user-attachments/assets/b3c32938-c1df-455c-be3e-3753c813535c)
 
-## Large text files
-Large text files slow QualCoder. When opening a large text file (greater than 50,000 characters), right click on the file. A menu option allows you to get the first approximately 50,000 characters, subsequent clicks will open the next approximately 50,000 and so on. Approximately, because QualCoder looks for a line-ending (such as a paragraph or sentence ending) to more nicely section the start and end of text chunks. If the 50,000 chunk still slows Qualcoder, in Settings you can reduce this character size further.
 
 ## Modifying code positions
 
-When in the text area, click on a code with the mouse (Note the code must not be overlapping with another code at that position). Press the following key combinations to extend or shrink the coded text segment.
+Right click on a code and select Resize. Handles at each end of the coded segment can be repositioned.
+
+<img width="599" height="77" alt="image" src="https://github.com/user-attachments/assets/8b9e09f0-03e0-4ad1-8042-f7aa5ce12b31" />
+
+Alteernatively, click on a code with the mouse (Note the code must not be overlapping with another code at that position). Press the following key combinations to extend or shrink the coded text segment.
 
 * Shift + left arrow            Extends coded text to the left
 * Shift + right arrow          Extends coded text to the right
@@ -140,33 +132,45 @@ When in the text area, click on a code with the mouse (Note the code must not be
 
 It is always best to save a copy of the project before editing the text, to avoid the need to edit coding positions, in case after editing text the coding positions are shifted incorrectly. The key strokes below may help to recover the correct positions.
 
-* Hash symbol - Shift + 3      Displays clicked character position.
-* Caret symbol ^  Shift + 6    Shift code positions, after the clicked character position. May be needed after the text is edited (added or deleted) to shift subsequent coding segments by a number of characters. 
+* Hash symbol  \#     Displays clicked character position.
+* Dollar symbol $   Shift code positions, after the clicked character position. May be needed after the text is edited (added or deleted) to shift subsequent coding segments by a number of characters. 
 
 
 ## Other shortcut keys for text coding
 
-* Shortcut A. Press a to Annotate selected text
+* A. Annotate selected text
+
+* B. Bookmark the current location in the current text file. Shift B to go to bookmark.
+
+* C. In text area. Create a category.
+  
+* Ctrl E to enter and exit Edit mode.
+
+* H. Hide and show the top section of controls on the screen. This provides a bigger area to view the text and to code.
+
+* I. On a coded section to mark that code as important. Useful for identifying exemplars. Important marks can be removed by right-click and selecting Remove important mark. Important marked codes are bolded.
 
-* Shortcut B. Press b to bookmark the current location in the current text file.
+* L. 'Show codes Like', to show a shortlist in the codes tree.
+  
+* M. Memo a code. (Must click over a coded text segment)
 
-* Shortcut L. Press L to 'Show codes like', to show a shortlist in the codes tree.
+* N. In text area. Create new code.
 
-* Shortcut R. Press r to select Recent codes. (Must have a text selection)
+* O. Cycle through overlapping codes in situ. (Must click over an overlapping codes segment - denoted with an overline).
 
-* Shortcut Q. Press q to Quick code with the most recent code. (Must have a text selection)
+* Q. Quick code with the most recent code. (Must have a text selection)
 
-* Shortcut M. Press m to memo a code. (Must click over a coded text segment)
+* R. Select Recent codes. (Must have a text selection)
 
-* Shortcut O. Press o to cycle through overlapping codes in situ. (Must click over an overlapping codes segment - denoted with an overline).
+* Ctrl R. Reverse text direction. May be needed for Arabic or other Right to left languages.
 
-* Shortcut H. Press h will hide and unhide the top section of controls on the screen. This provides a bigger area to view the text and to code.
+* S. Search text.
 
-* Shortcut I. Press i when on a coded section to mark that code as important. Useful for identifying exemplars. Important marks can be removed by right-click and selecting Remove important mark. Important marked codes are bolded.
+* R. Opens Recently used codes for marking text.
 
-* Shortcut U. Press u to unmark one or more codes at a clicked text position. (QC Release 3.4)
+* U. Unmark one or more codes at a clicked text position. (QC Release 3.4)
 
-* Shortcut V. Press v to code selected text with a new code made from the selected text. Also called *in vivo* coding.
+* V. Code selected text with a new code made from the selected text. Also called *in vivo* coding.
 
 * Ctrl Z. Restores the last coding that was unmarked.
 
@@ -192,7 +196,7 @@ Select a code and then click on the forward and back arrows. These now traverse
 
 Under the codes tree is a button with a star symbol. This button toggles between showing all coded text and the coded text flagged as Important.
 
-## Right-hand hidden pane
+## Right-hand pane
 
 The right-hand side of the coding text pane has another pane tucked away. To open click and drag from the right-hand side of the window, or press the button next to the Help button. This pane contains options useful when coding. One button is when clicking on a code, the code name, code rule (memo) and random coded text examples from the project are displayed. The other button displays the project memo.
 
@@ -224,7 +228,7 @@ Below is how the edit text screen looks.
 
 #  Categories and codes
 
-Categories are used to organise codes. Categories are organised hierarchically in a tree structure. You can move codes into categories and move categories into larger categories. You can move categories and codes out of their current position. Codes and categories can be merged by dropping a code onto a code or a category onto a category. Right-click on a category or code to rename. Right click on a code to change the colour.
+Categories are used to organise codes. Categories are organised hierarchically in a tree structure. You can move codes into categories and move categories into larger categories. You can move categories and codes out of their current position. A code can have sub-codes. Codes and categories can be merged by dropping a code onto a code or a category onto a category. Right-click on a category or code to rename. Right click on a code to change the colour.
 
 Categories and codes can be assigned memos. Memos can contain symbols such as those [shown here](https://www.alt-codes.net//). See the how-to link to insert them - Laptop num lock on, hold the Alt key down then enter the number using the number keypad on the right.
 
@@ -233,17 +237,17 @@ Categories and codes can be assigned memos. Memos can contain symbols such as th
     
 ## Reducing the amount of codes displayed in the codes tree
 
-To reduce the number of codes shown in the code tree. In the coding windows (code text, code, a/v or code image), right-click on the code tree and select _Show codes like_ from the menu. Then enter text in the text box. This will then only show codes that contain that text. To show all the codes again, enter nothing into the text box and press OK to show all the codes.
+To reduce the number of codes shown in the code tree. In the coding windows (code text, code, a/v or code image), right-click on the code tree and select _Show codes like_ from the menu, or when the codes tree is in focus key press F9.. Then enter text in the text box. This will then only show codes that contain that text. To show all the codes again, enter nothing into the text box and press OK to show all the codes.
 
-A second option is to _Show codes of colour_.
+A second option is to _Show codes of colour_. Key press F10 when codes tree is in focus.
 
 Another button under the code tree - with a search icon - on clicking will search for the first code that contains matching text that you enter.
 
 ## Codes tree menu options
 
-In the codes tree on the left, right clicking on a code opens a context menu for various options.
+In the codes tree on the left, right clicking on a code opens a context menu for various options. Sub-menus include modifying a code or category, filtering and sorting.
 
-![image](https://github.com/user-attachments/assets/0c0c97fb-a316-4076-a5c1-1fddd575e1f1)
+<img width="505" height="493" alt="image" src="https://github.com/user-attachments/assets/4adaa97e-da9c-45bf-88f3-8e0110edf638" />
 
 ###  Show coded files
 
@@ -253,6 +257,7 @@ With show coded files, you can right-click on the coloured header section for mo
 
 ![image](https://github.com/user-attachments/assets/7f0d079d-c0a8-4966-87bd-a6723f541d8d)
 
+## Sort the codes tree acending (key F11 when codes tree in focus), or descending (key F12 when tree in focus).
 
 ## Accessibility and code colours
 
diff --git a/docs/doc/en/4.3.-Coding-Text-on-PDFs.md b/docs/doc/en/4.3.-Coding-Text-on-PDFs.md
index d79347a..1245f4f 100644
--- a/docs/doc/en/4.3.-Coding-Text-on-PDFs.md
+++ b/docs/doc/en/4.3.-Coding-Text-on-PDFs.md
@@ -4,41 +4,10 @@ path: 4.3.-Coding-Text-on-PDFs
 
 # Coding PDFs
 
-PDFs can be displayed similarly to the original PDF. The text on the PDF can be coded. PDFs displayed with QualCoder are not exact replicas, but are re-displayed with some limitations described below. PDF documents take a long time to read, and larger PDFs will take even longer to load, so you may wait a few seconds for small PDFs and many minutes for large PDFs. I would recommend only coding the plain text of very large PDFs - via the code text option. Secondly, I would recommend not having PDFs larger than 20 or so pages. 
+The text and image areas on the PDF can be coded. 
 
-The screen is split into 3 adjustable panes. The left-hand side pane contains the file list and code tree. The middle pane shows the PDF representation. The right-hand pane shows the plain text extracted from the PDF. 
+<img width="1000" height="700" alt="image" src="https://github.com/user-attachments/assets/ba02d9ee-24fe-4b67-84b0-4f62cd62a1c6" />
 
-![Code PDF window](https://github.com/user-attachments/assets/4e13a708-ca8d-4c27-a736-60be5c132fcd)
-
-## Font size and zoom
-
-The original fonts for the text are not used; it is too hard to extract and apply them from the PDF. Instead, the standard font used within QualCoder is used in the displayed PDF text boxes. There is a Font size adjuster to shrink the font size, to try to avoid text overlapping. It is set at -2, and -3 may be quite good also. The PDF can be zoomed in and out via buttons or key presses: Ctrl - and Shift + 
-![font and zoom buttons](https://github.com/user-attachments/assets/87079c0f-a6e9-4b0f-b5ba-8f15bcd55411)
-
-## Plain text extract must match
-
-The plain text loaded when importing a PDF via Manage files must exactly match the plain text reloaded when the PDF is loaded to display it here. This means that PDFs loaded before version 3.4 will not match text correctly, and coding via selecting text boxes  is switched off if the text lengths do not match. Similarly, if the plain text is edited in any way and the text lengths do not match, the coding via selecting PDF text boxes is switched off. Display of the coding stripes within the  presented PDF  page may also look incorrect.
-
-## Navigation
-
-Move through the PDF pages by clicking on the page up-down arrows, or typing in a page number, using the buttons: 
-![page buttons](https://github.com/user-attachments/assets/58b64602-bf7a-421b-9da0-041893ba88db)
-
-## Extra functions 
-
-At the top of the window are several buttons. One exports an image of the PDF page. The magnifying glass button opens a window that shows the PDF elements used on the page.
-
-Also, at the top of the window are several check boxes to show or hide PDF elements of Text, Images. A checkbox will display all text in black, which may be beneficial when light-coloured text is shown on a light background.
-
-## Right-hand pane
-
-The right-hand side pane shows the plain text in the same way the text would be displayed in Code text. You can mark, unmark, memo the coded text, mark the coding as important, and annotate text here.
-
-## Central pane
-
-To code, left-click and drag the mouse to select PDF text boxes. This can be tricky depending on the size and positions of the boxes. Then click a code to assign the selection to it. Further adjustments can be made on the right-hand plain-text pane using the shift left or right arrows to extend, or the alt left and right arrows to shrink the coded text section.
-
-Right-click for the context menu on a coded segment. This menu allows you to add a memo, mark important, or unmark the coded segment.
 
 ### Key presses
 
@@ -56,8 +25,6 @@ Ctrl 4 View and edit file memo.
 
 Ctrl 5 Get files list from attributes.
 
-Ctrl 8 
-
 Ctrl 9 Show important coded segments. Toggles on and off.
 
 Ctrl 0 Show Help page in browser.
@@ -86,3 +53,20 @@ U Unmark at the selected location
 
 V assign 'in vivo' code to selected text
 
+## Older versions 3.8.2 and earlier
+
+The screen is split into 3 adjustable panes. The left-hand side pane contains the file list and code tree. The middle pane shows the PDF representation. The right-hand pane shows the plain text extracted from the PDF. The original fonts for the text are not used. Instead, the standard font within QualCoder is used in the displayed PDF text boxes. There is a Font size adjuster to shrink the font size, to try to avoid text overlapping. It is set at -2, and -3 may be quite good also. 
+
+![Code PDF window](https://github.com/user-attachments/assets/4e13a708-ca8d-4c27-a736-60be5c132fcd)
+
+### Right-hand pane
+
+The right-hand side pane shows the plain text in the same way the text would be displayed in Code text. You can mark, unmark, memo the coded text, mark the coding as important, and annotate text here.
+
+### Central pane
+
+To code, left-click and drag the mouse to select PDF text boxes. This can be tricky depending on the size and positions of the boxes. Then click a code to assign the selection to it. Further adjustments can be made on the right-hand plain-text pane using the shift left or right arrows to extend, or the alt left and right arrows to shrink the coded text section.
+
+Right-click for the context menu on a coded segment. This menu allows you to add a memo, mark important, or unmark the coded segment.
+
+
diff --git a/docs/doc/en/4.6.-Code-Organiser.md b/docs/doc/en/4.6.-Code-Organiser.md
new file mode 100644
index 0000000..c1c8354
--- /dev/null
+++ b/docs/doc/en/4.6.-Code-Organiser.md
@@ -0,0 +1,38 @@
+---
+path: 4.6-Code-Organiser
+---
+
+# Code Organiser
+
+The code organiser allows you change the structure of the coding tree.
+
+When you first open, you will be asked to back up your project before making or applying any changes. Changes made here cannot be undone.
+
+![image](https://github.com/user-attachments/assets/ae81b366-54e8-457f-9afe-abb7f76d2a41)
+
+Using the left-most button, select all code branches in your project, or a specific code branch only.
+
+![image](https://github.com/user-attachments/assets/57b82319-1145-4872-a6c7-63cbc807b370)
+
+All the sub-categories and codes from your selection will be displayed.
+
+![image](https://github.com/user-attachments/assets/303ddd26-605c-4963-9db4-ba7c50c40250)
+
+**Codes:**
+
+Right-click on a code to get options of: Removing from category, moving to another category, merging into another code, view/edit the memo, renaming, viewing the coded media, displaying the memo under the code name.
+
+**Categories:**
+
+There is a button at the top of the screen to add a new category. 
+
+Right-click on a category to: merge a category into another category, move a category under another category, remove a category from under another category, rename a category, view and edit memo, or just display the memo under the category name.
+
+Displaying the memo under the category name
+
+![image](https://github.com/user-attachments/assets/92fee31b-1b70-4bdf-bb15-51b0485e4739)
+
+There is a button to export the image as a PNG file.
+
+The Apply button makes the changes to the coding tree structure. If not pressed, the changes are not enacted.
+
diff --git a/docs/doc/en/4.7.-Codes-Tree.md b/docs/doc/en/4.7.-Codes-Tree.md
new file mode 100644
index 0000000..435fe0f
--- /dev/null
+++ b/docs/doc/en/4.7.-Codes-Tree.md
@@ -0,0 +1,38 @@
+#  Categories and codes
+
+Categories are used to organise codes. Categories are organised hierarchically in a tree structure. You can move codes into categories and move categories into larger categories. You can move categories and codes out of their current position. A code can have sub-codes. Codes and categories can be merged by dropping a code onto a code or a category onto a category. Right-click on a category or code to rename. Right click on a code to change the colour.
+
+Categories and codes can be assigned memos. Memos can contain symbols such as those [shown here](https://www.alt-codes.net//). See the how-to link to insert them - Laptop num lock on, hold the Alt key down then enter the number using the number keypad on the right.
+
+!!! note
+    The ability to assign a single code to multiple categories is a frequently requested feature. However, we have decided against implementing this functionality, as it would be overly complicated. This is due to QualCoder’s use of a tree structure, while the requested feature requires a [graph data structure](https://www.baeldung.com/cs/tree-vs-graph-data-structure). As an alternative, you can use codes with similar names across your different categories.
+    
+## Reducing the amount of codes displayed in the codes tree
+
+To reduce the number of codes shown in the code tree. In the coding windows (code text, code, a/v or code image), right-click on the code tree and select _Show codes like_ from the menu, or when the codes tree is in focus key press F9.. Then enter text in the text box. This will then only show codes that contain that text. To show all the codes again, enter nothing into the text box and press OK to show all the codes.
+
+A second option is to _Show codes of colour_. Key press F10 when codes tree is in focus.
+
+Another button under the code tree - with a search icon - on clicking will search for the first code that contains matching text that you enter.
+
+## Codes tree menu options
+
+In the codes tree on the left, right clicking on a code opens a context menu for various options. Sub-menus include modifying a code or category, filtering and sorting.
+
+<img width="505" height="493" alt="image" src="https://github.com/user-attachments/assets/4adaa97e-da9c-45bf-88f3-8e0110edf638" />
+
+###  Show coded files
+
+One option is Show coded files - this shows everywhere you have coded with the selected code. Clicking on a particular coded section will open that code in the original context for further insight.
+
+With show coded files, you can right-click on the coloured header section for more options such as adding more codes, removing the code, and export the entire set of coded segments to a text file.
+
+![image](https://github.com/user-attachments/assets/7f0d079d-c0a8-4966-87bd-a6723f541d8d)
+
+## Sort the codes tree acending (key F11 when codes tree in focus), or descending (key F12 when tree in focus).
+
+## Accessibility and code colours
+
+In the Coding menu there is an option called **Colour scheme**.
+
+You can apply a range of colours to a range of selected codes, rather than rely on the random generated colours or a one at a time change of colours for each code. In this area you can also view how the code colours may appear to someone who has a variety of colour blindness: red-weak, red blind, green-weak or green blind.
\ No newline at end of file
diff --git a/docs/doc/en/6.1.-Imports-and-Exports.md b/docs/doc/en/6.1.-Imports-and-Exports.md
index 4a1d258..d7f5bcc 100644
--- a/docs/doc/en/6.1.-Imports-and-Exports.md
+++ b/docs/doc/en/6.1.-Imports-and-Exports.md
@@ -59,13 +59,6 @@ If no coded data is visible, you may need to change the current coder's name in
 
 This is now old (Feb 2021), but a user tested the importation of a REFI-QDA project file generated by QualCoder and imported into MAXQDA, Quirkos, Atlas.ti. Only text and Images were tested. A PDF of the test results is here: [Testing importation of REFI-QDA Project files into Atlas, MAXQDA, Quirkos PDF](https://qualcoder.files.wordpress.com/2021/02/refi-qda-project-import-quirkos-maxqda-atlas.pdf)
 
-## RQDA
-Projects made with [RQDA](http://rqda.r-forge.r-project.org/) can be directly imported. All data except for file categories are imported.
-
-Close any opened project. Then in the Main Menu under Project click on RQDA Project import. You will be asked to create a new project- so enter a name. Then you will be asked to select the RQDA project file. QualCoder will then import the data.
-
-If no coded data is visible, you may need to change the current coder's name in Settings. Try the drop down coder name box.
-
 ## Taguette
 Projects created with [Taguette](https://www.taguette.org/) can be imported directly. All your core coding data will be transferred.
 
@@ -73,7 +66,18 @@ Close any open project. Then, in the Main Menu under Project, click on Taguette
 
 If you do not see any coded data, you may need to change the current coder's name in the Settings. Try using the coder name drop-down box.
 
-## Survey import
+## Sonal π (Sonal Pi)
+
+Projects created with Sonal Pi a French developed qualitative Data Analysis Software can be imported. [https://www.sonal-info.com](https://www.sonal-info.com)
+
+## RQDA
+Projects made with [RQDA](http://rqda.r-forge.r-project.org/) can be directly imported. All data except for file categories are imported.
+
+Close any opened project. Then in the Main Menu under Project click on RQDA Project import. You will be asked to create a new project- so enter a name. Then you will be asked to select the RQDA project file. QualCoder will then import the data.
+
+If no coded data is visible, you may need to change the current coder's name in Settings. Try the drop down coder name box.
+
+## Survey import- To Reivew
 
 This requires a CSV or Excel (xlsx format) file. You can try importing the survey.csv file in the Examples folder.
 
@@ -117,22 +121,7 @@ Open Manage Files, then View that file, and you will see the case-assignment tex
 
 ## Bibliography import
 
-Import a bibliography. This must be in RIS formatted plain text. The file can have endings of .txt or .ris
-
-In the menu, go to Manage > Manage references. Press the import button: This has the icon of an arrow on top of a document.
-
-Select the file.
-
-The Action Log tab will show how many references were imported.
-
-Once imported, go to the menu: Files and Cases > Manage references.
-
-In here you can link (and unlink) references to files. You can manually edit an imported reference. You can delete a reference.
-
-Once this is imported and files are linked to references, go to the Manage Files screen.
-In here there will be four new attribute columns: Ref_Authors, Ref_Title, Ref_Type, Ref_Year
-
-Files linked to references will have details in those columns.
+See [Managing Data - References](https://qualcoder.org/doc/en/3.5.-References/) section.
 
 ## Help - Special Functions
 
diff --git a/docs/using.md b/docs/using.md
index 3b9d7c6..bf944db 100644
--- a/docs/using.md
+++ b/docs/using.md
@@ -2,6 +2,8 @@
 
 These are records that I have found on the internet, or that authors have informed me about. Studies, reports, abstracts, conference proceeding, datasets are listed from newest to oldest :
 
+466 Kinnig, T., Rohmer, O., Timera, A., & Sanrey, C. (2026). **Poursuivre ses études à l’université: qu’en disent les étudiant· es en situation de handicap invisible?. L'orientation scolaire et professionnelle (Pursuing university studies: what do students with invisible disabilities say? Educational and vocational guidance.)**, (55/2), 265-290. <https://doi.org/10.4000/16da8>
+
 465 Nguyen, Q. K. (2026). **Comparing Learners’ Perceptions of AI-Enabled Writing Feedback and Human Teacher Feedback in IELTS Writing Preparation.** Asian Journal of Educational Sciences, 1(2), 62-83. <https://doi.org/10.52714/dthu.ajes.27.2022>
 
 464 Walmrath, L. L., & Barro, B. B. (2026). **A eletrificação automotiva brasileira: uma análise socioeconômica das recentes transformações tecnológicas (Automotive electrification in Brazil: a socio-economic analysis of recent technological changes).** TOMO Review, 45, e24789. <https://doi.org/10.21669/tomo.v45.24789>
diff --git a/zensical.toml b/zensical.toml
index f65090f..e048c17 100644
--- a/zensical.toml
+++ b/zensical.toml
@@ -47,7 +47,8 @@ nav = [
 				"doc/en/3.1.-Creating-a-New-Project.md",  
 				"doc/en/3.2.-Files.md",  
 				"doc/en/3.3.-Cases.md",  
-				"doc/en/3.4.-Attributes.md" 
+				"doc/en/3.4.-Attributes.md",
+				"doc/en/3.5.-References.md"
 		]},
 		{"Coding" = [
 			"doc/en/4.1.-Coding-Text.md",  
@@ -55,7 +56,8 @@ nav = [
 			"doc/en/4.3.-Coding-Text-on-PDFs.md",  
 			"doc/en/4.4.-Coding-Images.md",  
 			"doc/en/4.5.-Coding-Audio-and-Video.md",  
-			"doc/en/4.6-Code-Organiser.md"
+			"doc/en/4.6.-Code-Organiser.md",
+			"doc/en/4.7.-Codes-Tree.md"
 		]},
 		{"Analyzing the Results" = [
 			"doc/en/5.1.-AI-chat-based-analysis.md",  
